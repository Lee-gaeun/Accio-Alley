<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.review.mapper.ReviewMapper">

    <!-- 리뷰 등록 -->
    <insert id="insertReview" parameterType="org.scoula.review.domain.ReviewVO">
        INSERT INTO review (user_id, product_id, rating, coment)
        VALUES (#{userId}, #{productId}, #{rating}, #{coment})
    </insert>

    <!-- 특정 상품 리뷰 목록 조회 -->
    <select id="selectByProductId" parameterType="long" resultType="org.scoula.review.domain.ReviewVO">
        SELECT review_id, user_id, product_id, rating, coment
        FROM review
        WHERE product_id = #{productId}
    </select>

    <!-- 리뷰 수정 -->
    <update id="updateReview" parameterType="org.scoula.review.domain.ReviewVO">
        UPDATE review
        SET rating = #{rating}, coment = #{coment}
        WHERE review_id = #{reviewId}
    </update>

    <!-- 리뷰 삭제 -->
    <delete id="deleteReview" parameterType="long">
        DELETE FROM review
        WHERE review_id = #{reviewId}
    </delete>

</mapper>